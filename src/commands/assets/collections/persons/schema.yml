$meta:
  type: Schema
  title: "Person Concept Schema"
  description: "Defines the custom fields extracted into summary frontmatter for Person entities."

persons: [string] # Array | Required | List of individuals extracted from source.
relationships: [string] # Array | Required | List of relationships between extracted persons (e.g. personA, relation, personB).
